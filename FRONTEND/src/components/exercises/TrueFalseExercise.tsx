import type { TrueFalseExercise as Exercise, TrueFalseResult } from "../../types/exercise";
export function TrueFalseExercise({ exercise, result, isSubmitting, onSubmit, onNext }: { exercise: Exercise; result: TrueFalseResult | null; isSubmitting: boolean; onSubmit: (answer: { value: boolean }) => void; onNext: () => void }) { return <article className="exercise-card"><header className="exercise-card-heading"><span>{exercise.payload.groupName}</span><h3>{exercise.payload.statement}</h3></header>{!result ? <div className="review-actions"><button className="review-answer-button review-answer-false" disabled={isSubmitting} onClick={() => onSubmit({ value: false })}>Falso</button><button className="review-answer-button review-answer-true" disabled={isSubmitting} onClick={() => onSubmit({ value: true })}>Verdadeiro</button></div> : <div className={`exercise-feedback ${result.isCorrect ? "exercise-feedback-correct" : "exercise-feedback-incorrect"}`}><strong>{result.isCorrect ? "Correto" : `Incorreto. A afirmação é ${result.correctAnswer ? "verdadeira" : "falsa"}.`}</strong>{result.explanation && <p>{result.explanation}</p>}{result.reference && <p>Referência: {result.reference}</p>}<button className="exercise-primary-button" onClick={onNext}>Próximo exercício</button></div>}</article>; }
